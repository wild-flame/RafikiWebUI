// import { Types } from "./actions"


const initialState = {
  auth: {
    uid: "user123"
  }
};

export const auth = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
